{
  "name": "draw-app",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": "22.x",
    "pnpm": "9.x"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prisma:migrate": "cd packages/db && npx prisma migrate deploy",
    "prisma:generate": "cd packages/db && npx prisma generate",
    "build:http": "npm run prisma:migrate && npm run prisma:generate && cd apps/http-backend && pnpm install && pnpm run build",
    "start:http": "cd apps/http-backend && pnpm run start",
    "build:ws": "npm run prisma:migrate && npm run prisma:generate && cd apps/ws-backend && pnpm install && pnpm run build",
    "start:ws": "cd apps/ws-backend && pnpm run start"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  }
}

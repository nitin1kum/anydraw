{
  "projects": {
    "excelidraw-frontend": {
      "rootDirectory": "apps/excelidraw-frontend",
      "framework": "nextjs",
      "buildCommand": "corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm install --no-frozen-lockfile && pnpm run build",
      "installCommand": "",
      "outputDirectory": ".next",
      "env": {
        "COREPACK_ENABLE_STRICT": "0"
      }
    }
  }
}
